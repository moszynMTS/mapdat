import React from "react";
import { View } from "react-native";

export const ModalInfoItem: React.FC<{ item: string | null;}> = ({ item }) => {

    return(
        <View>
            
        </View>
    )
}
